import type { AccountingTransaction, PostingResult, VoucherLineInput } from "./types";
import { postVoucher, type VoucherEngineDeps } from "./voucher";
import { ValidationError } from "./errors";
export async function postIdempotentVoucher(tx:AccountingTransaction,input:{businessId:string;financialYearId:string;voucherType:string;date:string;narration?:string;referenceType?:string;referenceId?:string;prefix?:string;createdBy:string;lines:VoucherLineInput[];idempotencyKey:string},deps:VoucherEngineDeps):Promise<PostingResult>{if(!input.idempotencyKey)throw new ValidationError("Idempotency key is required for an atomic business command.");const existing=await tx.getVoucherByIdempotencyKey(input.businessId,input.financialYearId,input.idempotencyKey);if(existing){if(existing.businessId!==input.businessId||existing.financialYearId!==input.financialYearId||existing.voucherType!==input.voucherType)throw new ValidationError("Idempotency key is already used by a different accounting operation.");const lines=await tx.getVoucherLines(existing.id);return{voucher:existing,lines,ledgerEntries:lines.map(l=>({...l,date:existing.date,voucherType:existing.voucherType,voucherNumber:existing.voucherNumber,createdAt:existing.createdAt})),stockMovements:await tx.getStockMovementsForSource(existing.id)};}const result=await postVoucher(tx,input,deps);result.voucher.idempotencyKey=input.idempotencyKey;await tx.saveVoucher(result.voucher);return result;}
