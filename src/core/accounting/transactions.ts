import type { AccountingRepository, AtomicAccountingDocument, Money, PostingResult, VoucherLineInput } from "./types";
import { postIdempotentVoucher } from "./atomic";
import { postVoucher } from "./voucher";
import { createStockMovement } from "./inventory";
import { buildSerialStock } from "./inventoryTracking";
import { calculateTax } from "./gst";
import { ValidationError } from "./errors";
import { assertMoney, assertQuantity } from "./money";
import { partyLedgerAccountId } from "./partyMaster";
import { addCreditDays } from "./partyFeatures";
export interface TransactionDeps { ids:{next(prefix:string):string}; clock:{now():string} }
export interface BaseTransaction { businessId:string; financialYearId:string; date:string; userId:string; narration?:string }
export interface AccountMap { party:string;sales?:string;purchases?:string;cash?:string;bank?:string;inputCgst?:string;inputSgst?:string;inputIgst?:string;outputCgst?:string;outputSgst?:string;outputIgst?:string;outputCess?:string;inputCess?:string;inventory?:string;cogs?:string }
const debit=(accountId:string,amount:Money,extra:Partial<VoucherLineInput>={})=>({accountId,debit:amount,credit:0,...extra});
const credit=(accountId:string,amount:Money,extra:Partial<VoucherLineInput>={})=>({accountId,debit:0,credit:amount,...extra});
const required=(value:string|undefined,name:string)=>{if(!value)throw new ValidationError(`Missing ${name} account.`);return value};
function validateBase(base:BaseTransaction){if(!base.businessId||!base.financialYearId||!base.userId)throw new ValidationError("Business, financial year and user are required.");if(!/^\d{4}-\d{2}-\d{2}$/.test(base.date))throw new ValidationError("Transaction date must be YYYY-MM-DD.")}
interface StockCommand {itemId:string;quantity:number;unitCost?:Money;warehouseId?:string;batchId?:string;batchNumber?:string;manufactureDate?:string;expiryDate?:string;serialNumbers?:string[];unitId?:string;quantityInBaseUnit?:number}
function validateStockItems(items:readonly StockCommand[]){if(!items.length)throw new ValidationError("At least one stock item is required.");const ids=new Set<string>();for(const item of items){if(!item.itemId)throw new ValidationError("Stock item ID is required.");assertQuantity(item.quantity);if(item.unitCost!==undefined)assertMoney(item.unitCost,"Unit cost");if(item.serialNumbers&&item.serialNumbers.length!==item.quantity)throw new ValidationError("Serial number count must equal stock quantity.");const key=`${item.itemId}:${item.warehouseId??""}:${item.batchId??""}`;if(ids.has(key))throw new ValidationError(`Duplicate stock line: ${key}`);ids.add(key)}}
function stockValue(items:readonly StockCommand[]):Money{let value=0;for(const item of items){if(item.unitCost===undefined)throw new ValidationError("Unit cost is required for purchase stock lines.");const line=Math.round(item.quantity*item.unitCost);if(!Number.isSafeInteger(line))throw new ValidationError("Stock line value exceeds safe integer range.");value+=line}if(!Number.isSafeInteger(value))throw new ValidationError("Stock value exceeds safe integer range.");return value}
function atomicDocument(input:{id:string;businessId:string;financialYearId:string;type:AtomicAccountingDocument["type"];voucherId:string;idempotencyKey:string;date:string;createdBy:string;createdAt:string;payload:Record<string,unknown>}):AtomicAccountingDocument{return{...input,status:"posted"}}
export async function postJournal(repo:AccountingRepository,base:BaseTransaction,lines:VoucherLineInput[],deps:TransactionDeps,voucherType="JOURNAL",prefix="JV"):Promise<PostingResult>{validateBase(base);return repo.runInTransaction(tx=>postVoucher(tx,{businessId:base.businessId,financialYearId:base.financialYearId,voucherType,prefix,date:base.date,narration:base.narration,createdBy:base.userId,lines},deps))}
export async function postReceipt(repo:AccountingRepository,base:BaseTransaction&{partyId:string;amount:Money;mode:"cash"|"bank";accountMap:AccountMap},deps:TransactionDeps){if(!Number.isSafeInteger(base.amount)||base.amount<=0)throw new ValidationError("Receipt amount must be positive.");return postJournal(repo,base,[debit(required(base.mode==="cash"?base.accountMap.cash:base.accountMap.bank,`${base.mode} account`),base.amount),credit(base.accountMap.party,base.amount,{partyId:base.partyId})],deps,"RECEIPT","RC")}
export async function postPayment(repo:AccountingRepository,base:BaseTransaction&{partyId:string;amount:Money;mode:"cash"|"bank";accountMap:AccountMap},deps:TransactionDeps){if(!Number.isSafeInteger(base.amount)||base.amount<=0)throw new ValidationError("Payment amount must be positive.");return postJournal(repo,base,[debit(base.accountMap.party,base.amount,{partyId:base.partyId}),credit(required(base.mode==="cash"?base.accountMap.cash:base.accountMap.bank,`${base.mode} account`),base.amount)],deps,"PAYMENT","PY")}
export async function postContra(repo:AccountingRepository,base:BaseTransaction&{fromAccountId:string;toAccountId:string;amount:Money},deps:TransactionDeps){if(!Number.isSafeInteger(base.amount)||base.amount<=0)throw new ValidationError("Contra amount must be positive.");return postJournal(repo,base,[debit(base.toAccountId,base.amount),credit(base.fromAccountId,base.amount)],deps,"CONTRA","CT")}
export interface OpeningBalanceInput extends BaseTransaction {accountId:string;amount:Money;side:"debit"|"credit";accountMap:{openingBalance:string}}
export async function postOpeningBalance(repo:AccountingRepository,input:OpeningBalanceInput,deps:TransactionDeps){if(!Number.isSafeInteger(input.amount)||input.amount<=0)throw new ValidationError("Opening balance amount must be positive.");const clearing=required(input.accountMap.openingBalance,"opening balance");const lines=input.side==="debit"?[debit(input.accountId,input.amount),credit(clearing,input.amount)]:[debit(clearing,input.amount),credit(input.accountId,input.amount)];return postJournal(repo,input,lines,deps,"OPENING","OB")}
export async function postExpense(repo:AccountingRepository,base:BaseTransaction&{expenseAccountId:string;amount:Money;mode:"cash"|"bank";accountMap:AccountMap},deps:TransactionDeps){if(!Number.isSafeInteger(base.amount)||base.amount<=0)throw new ValidationError("Expense amount must be positive.");return postJournal(repo,base,[debit(base.expenseAccountId,base.amount),credit(required(base.mode==="cash"?base.accountMap.cash:base.accountMap.bank,`${base.mode} account`),base.amount)],deps,"EXPENSE","EX")}
export interface PurchasePostingInput extends BaseTransaction {supplierId?:string;taxableValue:Money;grossValue?:Money;discountPercent?:number;discountAmount?:Money;taxRate:number;intraState:boolean;cessRate?:number;mode:"credit"|"cash"|"bank";paidAmount?:Money;supplierInvoiceNumber?:string;accountMap:AccountMap;itemMovements:StockCommand[];idempotencyKey:string;documentId?:string;documentPayload?:Record<string,unknown>}
function normalizedPurchaseTaxable(input:PurchasePostingInput):{gross:Money;discount:Money;taxable:Money;discountPercent:number}{const taxable=input.taxableValue;if(!Number.isSafeInteger(taxable)||taxable<=0)throw new ValidationError("Purchase taxable value must be positive.");const discountAmount=input.discountAmount??0;if(!Number.isSafeInteger(discountAmount)||discountAmount<0)throw new ValidationError("Purchase discount amount is invalid.");const discountPercent=input.discountPercent??0;if(!Number.isFinite(discountPercent)||discountPercent<0||discountPercent>100)throw new ValidationError("Purchase discount percentage must be between 0 and 100.");if(input.grossValue===undefined&&discountPercent>0)throw new ValidationError("Gross purchase value is required when a percentage discount is used.");const gross=input.grossValue??taxable+discountAmount;if(!Number.isSafeInteger(gross)||gross<=0)throw new ValidationError("Purchase gross value is invalid.");const percentDiscount=Math.round(gross*discountPercent/100);const totalDiscount=percentDiscount+discountAmount;if(totalDiscount>gross)throw new ValidationError("Purchase discount cannot exceed gross value.");const derived=gross-totalDiscount;if(derived!==taxable)throw new ValidationError("Purchase gross, discount and taxable values do not reconcile.");return{gross,discount:totalDiscount,taxable:derived,discountPercent}}
export async function postPurchase(repo:AccountingRepository,input:PurchasePostingInput,deps:TransactionDeps):Promise<PostingResult>{validateBase(input);if(!input.idempotencyKey)throw new ValidationError("Idempotency key is required.");const totals=normalizedPurchaseTaxable(input);if(input.mode==="credit"&&!input.supplierId)throw new ValidationError("Supplier is required for a credit purchase.");if(input.mode==="credit"&&input.paidAmount!==undefined&&input.paidAmount!==0)throw new ValidationError("A credit purchase bill cannot use a cash/bank settlement; record payment through the Payment Engine.");validateStockItems(input.itemMovements);const stockTotal=stockValue(input.itemMovements);if(stockTotal!==totals.taxable)throw new ValidationError(`Purchase stock value (${stockTotal}) must equal taxable value (${totals.taxable}).`);const supplierInvoiceNumber=input.supplierInvoiceNumber?.trim();if(supplierInvoiceNumber&&supplierInvoiceNumber.length>100)throw new ValidationError("Supplier invoice number is too long.");return repo.runInTransaction(async tx=>{const pre=await tx.getVoucherByIdempotencyKey(input.businessId,input.financialYearId,input.idempotencyKey);if(pre)return{voucher:pre} as PostingResult;let supplierLedger:string|undefined;let dueDate:string|undefined;if(input.supplierId){const raw=await tx.getBusinessDocument("parties",input.supplierId);if(!raw)throw new ValidationError("Selected supplier does not exist in the canonical Party Master.");const party=raw as {businessId?:string;kind?:"customer"|"supplier"|"both";status?:string;ledgerAccountId?:string;payableLedgerAccountId?:string;features?:{paymentTermsId?:string}};if(party.businessId!==input.businessId||!(["supplier","both"].includes(String(party.kind))))throw new ValidationError("Selected party is not a supplier of this business.");if(party.status!=="active")throw new ValidationError("Selected supplier is inactive.");supplierLedger=partyLedgerAccountId(party,"supplier");if(supplierLedger!==input.accountMap.party)throw new ValidationError("Purchase party account does not match the supplier payable ledger account.");const termsId=party.features?.paymentTermsId;if(termsId){const terms=await tx.getBusinessDocument("paymentTerms",termsId);if(terms&&terms.businessId===input.businessId&&terms.active!==false)dueDate=addCreditDays(input.date,Number(terms.creditDays??0));}if(supplierInvoiceNumber){const key=`${input.supplierId}:${supplierInvoiceNumber.toUpperCase().replace(/\s+/g," ")}`;if(await tx.getBusinessDocument("purchaseSupplierInvoices",key))throw new ValidationError(`Supplier invoice ${supplierInvoiceNumber} is already recorded for this supplier.`);}}else if(supplierInvoiceNumber)throw new ValidationError("Supplier is required when a supplier invoice number is provided.");for(const item of input.itemMovements){if(item.serialNumbers?.length){const existing=buildSerialStock(await tx.getStockMovementsForItem(item.itemId,item.warehouseId,input.date),item.itemId,item.warehouseId);const held=new Set(existing.map(x=>x.serialNumber));for(const serial of item.serialNumbers)if(held.has(serial))throw new ValidationError(`Serial number ${serial} is already in stock.`);}}const tax=calculateTax({taxableValue:totals.taxable,rate:input.taxRate,intraState:input.intraState,cessRate:input.cessRate});const inventoryAccount=required(input.accountMap.inventory,"inventory");const paidValue=input.mode==="credit"?0:(input.paidAmount??tax.total);if(!Number.isSafeInteger(paidValue)||paidValue<0||paidValue>tax.total)throw new ValidationError("Purchase paid amount must be between zero and the purchase total.");const outstanding=tax.total-paidValue;if(outstanding>0&&!input.supplierId)throw new ValidationError("Supplier is required for an unpaid or partially paid purchase.");let settlementAccount:string|undefined;if(paidValue>0){settlementAccount=required(input.mode==="cash"?input.accountMap.cash:input.accountMap.bank,"purchase settlement");const account=await tx.getAccount(settlementAccount);if(!account||!account.active||account.type!=="asset")throw new ValidationError("Purchase cash/bank settlement account must be an active asset account.");}const lines:VoucherLineInput[]=[debit(inventoryAccount,stockTotal)];if(tax.cgst)lines.push(debit(required(input.accountMap.inputCgst,"input CGST"),tax.cgst));if(tax.sgst)lines.push(debit(required(input.accountMap.inputSgst,"input SGST"),tax.sgst));if(tax.igst)lines.push(debit(required(input.accountMap.inputIgst,"input IGST"),tax.igst));if(tax.cess)lines.push(debit(required(input.accountMap.inputCess,"input cess"),tax.cess));if(paidValue>0)lines.push(credit(settlementAccount!,paidValue));if(outstanding>0)lines.push(credit(required(supplierLedger??input.accountMap.party,"party account"),outstanding,{partyId:input.supplierId}));const result=await postIdempotentVoucher(tx,{businessId:input.businessId,financialYearId:input.financialYearId,voucherType:"PURCHASE",prefix:"PB",date:input.date,dueDate,narration:input.narration,createdBy:input.userId,referenceType:"purchase",referenceId:input.documentId??input.idempotencyKey,lines,idempotencyKey:input.idempotencyKey},deps);const movements=input.itemMovements.map(item=>createStockMovement({businessId:input.businessId,financialYearId:input.financialYearId,date:input.date,itemId:item.itemId,warehouseId:item.warehouseId,direction:"in",quantity:item.quantity,unitCost:item.unitCost!,sourceType:"purchase",sourceId:result.voucher.id,createdBy:input.userId,batchId:item.batchId,batchNumber:item.batchNumber,manufactureDate:item.manufactureDate,expiryDate:item.expiryDate,serialNumbers:item.serialNumbers,unitId:item.unitId,quantityInBaseUnit:item.quantityInBaseUnit},deps.ids,deps.clock.now()));await tx.saveStockMovements(movements);const payload={...(input.documentPayload??{}),businessId:input.businessId,purchaseId:input.documentId??result.voucher.id,accountingVoucherId:result.voucher.id,accountingVoucherNumber:result.voucher.voucherNumber,supplierInvoiceNumber:supplierInvoiceNumber??null,grossValue:totals.gross,discountPercent:totals.discountPercent,discountAmount:totals.discount,taxableValue:totals.taxable,taxTotal:tax.total,total:tax.total,paidAmount:paidValue,outstandingAmount:outstanding,partyId:input.supplierId??null,dueDate:dueDate??null};await tx.saveAtomicDocument(atomicDocument({id:input.documentId??result.voucher.id,businessId:input.businessId,financialYearId:input.financialYearId,type:"purchase",voucherId:result.voucher.id,idempotencyKey:input.idempotencyKey,date:input.date,createdBy:input.userId,createdAt:deps.clock.now(),payload}));if(input.documentId)await tx.saveBusinessDocument("purchases",input.documentId,payload);if(input.supplierId&&supplierInvoiceNumber){const key=`${input.supplierId}:${supplierInvoiceNumber.toUpperCase().replace(/\s+/g," ")}`;await tx.saveBusinessDocument("purchaseSupplierInvoices",key,{businessId:input.businessId,supplierId:input.supplierId,supplierInvoiceNumber,purchaseId:input.documentId??result.voucher.id,voucherId:result.voucher.id,financialYearId:input.financialYearId,createdAt:deps.clock.now()});}return{...result,stockMovements:movements};}
