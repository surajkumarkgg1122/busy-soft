"use client";

import { useCallback, useEffect, useMemo, useState } from "react";
import Sidebar from "../Components/Sidebar/page";
import TopNav from "../Components/TopNav/page";
import AuthGate from "../Components/Auth/AuthGate";
import { firebaseAuth } from "../../lib/firebase";
import { useBusiness } from "../../context/BusinessContext";

type Expense = { expenseId:string; expenseNumber?:string; categoryId?:string; amountMinor?:number; paymentMode?:string; date?:string; description?:string; accountingVoucherNumber?:string; accountingVoucherId?:string };
type Category = { id:string; name:string; code?:string };
const initialForm={expenseNumber:"",category:"General",amount:"",paymentMethod:"cash",date:new Date().toISOString().slice(0,10),description:"",bankAccountId:""};
const paymentNames:Record<string,string>={cash:"Cash",bank:"Bank",upi:"UPI / Digital Wallet",card:"Card",cheque:"Cheque",other:"Other"};
const money=(minor:number)=>`₹ ${(Number(minor||0)/100).toLocaleString("en-IN",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
async function authHeaders(){if(!firebaseAuth?.currentUser)throw new Error("You must be signed in.");return{Authorization:`Bearer ${await firebaseAuth.currentUser.getIdToken()}`,"Content-Type":"application/json"};}

export default function ExpensesPage(){
 const {activeBusinessId,activeBusiness,can}=useBusiness();const canView=can("expenses","view"),canCreate=can("expenses","create");
 const[expenses,setExpenses]=useState<Expense[]>([]);const[categories,setCategories]=useState<Category[]>([]);const[form,setForm]=useState(initialForm);const[search,setSearch]=useState("");const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[showForm,setShowForm]=useState(false);const[viewing,setViewing]=useState<Expense|null>(null);const[error,setError]=useState("");
 const load=useCallback(async()=>{if(!activeBusinessId){setExpenses([]);setLoading(false);return;}setLoading(true);try{const r=await fetch(`/api/accounting/expenses?businessId=${encodeURIComponent(activeBusinessId)}`,{headers:await authHeaders(),cache:"no-store"});const d=await r.json();if(!r.ok||!d.success)throw new Error(String(d.error??"Could not load expenses."));setExpenses(d.data.expenses as Expense[]);setCategories(d.data.categories as Category[]);setError("");}catch(e){setError(e instanceof Error?e.message:"Could not load expenses.");}finally{setLoading(false);}},[activeBusinessId]);
 useEffect(()=>{void load();},[load]);
 const filtered=useMemo(()=>expenses.filter(x=>`${x.expenseNumber??""} ${x.categoryId??""} ${x.description??""}`.toLowerCase().includes(search.toLowerCase())),[expenses,search]);
 const total=filtered.reduce((s,x)=>s+Number(x.amountMinor||0),0);const monthTotal=expenses.filter(x=>{const d=new Date(x.date||"");const n=new Date();return d.getMonth()===n.getMonth()&&d.getFullYear()===n.getFullYear();}).reduce((s,x)=>s+Number(x.amountMinor||0),0);
 function openCreate(){setForm({...initialForm,expenseNumber:String(Math.max(0,...expenses.map(x=>Number(x.expenseNumber)||0))+1).padStart(3,"0")});setError("");setShowForm(true);}
 async function save(e:React.FormEvent){e.preventDefault();if(!activeBusinessId)return setError("Select a business first.");const amount=Number(form.amount);if(!Number.isFinite(amount)||amount<=0)return setError("Enter an expense amount greater than zero.");if(form.paymentMethod!=="cash"&&!form.bankAccountId)return setError("Enter the configured bank account ID for non-cash expense.");setSaving(true);setError("");try{const r=await fetch("/api/accounting/expenses",{method:"POST",headers:await authHeaders(),body:JSON.stringify({businessId:activeBusinessId,expenseNumber:form.expenseNumber,category:form.category,amountMinor:Math.round(amount*100),paymentMethod:form.paymentMethod,date:form.date,description:form.description,bankAccountId:form.bankAccountId||undefined,idempotencyKey:`expense-${crypto.randomUUID()}`})});const d=await r.json();if(!r.ok||!d.success)throw new Error(String(d.error??"Could not post expense."));setForm(initialForm);setShowForm(false);await load();}catch(err){setError(err instanceof Error?err.message:"Could not post expense.");}finally{setSaving(false);}}
 if(!canView)return <AuthGate><div className="p-10 text-red-600">You do not have permission to view expenses.</div></AuthGate>;
 return <AuthGate><div className="flex min-h-screen bg-[var(--erp-background,#f7f8fc)]"><Sidebar/><main className="min-w-0 flex-1 px-4 pb-10 sm:px-6 lg:px-8"><TopNav/><div className="mx-auto max-w-[1450px] py-5"><header className="flex flex-col gap-4 border-b border-[var(--erp-border,#e4e7ec)] pb-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-[var(--erp-primary,#465fff)]">Expenses</p><h1 className="mt-1 text-2xl font-bold text-[var(--erp-text,#101828)]">Business Expenses</h1><p className="mt-1 text-sm text-[var(--erp-text-muted,#667085)]">Atomic expense vouchers through the Application Layer and Accounting Core.</p></div>{canCreate&&<button onClick={openCreate} className="erp-btn-primary h-10 rounded-lg px-4 text-sm font-semibold">+ Add Expense</button>}</header>{error&&<div role="alert" className="mt-4 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<div className="mt-5 grid gap-4 sm:grid-cols-3"><div className="erp-card rounded-xl p-4"><p className="text-xs text-[var(--erp-text-muted,#667085)]">Filtered total</p><p className="mt-2 text-xl font-bold">{money(total)}</p></div><div className="erp-card rounded-xl p-4"><p className="text-xs text-[var(--erp-text-muted,#667085)]">This month</p><p className="mt-2 text-xl font-bold">{money(monthTotal)}</p></div><div className="erp-card rounded-xl p-4"><p className="text-xs text-[var(--erp-text-muted,#667085)]">Entries</p><p className="mt-2 text-xl font-bold">{filtered.length}</p></div></div><section className="erp-card mt-5 overflow-hidden rounded-xl"><div className="flex flex-col gap-3 border-b border-[var(--erp-border,#e4e7ec)] p-4 lg:flex-row lg:items-center lg:justify-between"><div><h2 className="font-semibold">Expense Register</h2><p className="text-xs text-[var(--erp-text-muted,#667085)]">Posted expenses are immutable accounting transactions.</p></div><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search..." className="erp-input h-9 w-64 rounded-lg px-3 text-sm"/></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-sm"><thead className="bg-[#fafafa] text-xs uppercase tracking-wide text-[#667085]"><tr><th className="px-5 py-3 text-left">Expense No.</th><th className="px-5 py-3 text-left">Date</th><th className="px-5 py-3 text-left">Category</th><th className="px-5 py-3 text-left">Paid From</th><th className="px-5 py-3 text-left">Description</th><th className="px-5 py-3 text-right">Amount</th><th className="px-5 py-3 text-left">Voucher</th><th className="px-5 py-3 text-right">Action</th></tr></thead><tbody>{loading?<tr><td colSpan={8} className="px-5 py-12 text-center">Loading…</td></tr>:filtered.length===0?<tr><td colSpan={8} className="px-5 py-12 text-center text-[#98a2b3]">No expenses found.</td></tr>:filtered.map(x=><tr key={x.expenseId} className="border-t border-[#f2f4f7]"><td className="px-5 py-3 font-semibold">#{x.expenseNumber||x.expenseId.slice(0,8)}</td><td className="px-5 py-3">{x.date||"—"}</td><td className="px-5 py-3">{x.categoryId||"General"}</td><td className="px-5 py-3 capitalize">{paymentNames[x.paymentMode||"cash"]||x.paymentMode||"Cash"}</td><td className="max-w-[260px] truncate px-5 py-3 text-[#667085]">{x.description||"—"}</td><td className="px-5 py-3 text-right font-semibold">{money(Number(x.amountMinor||0))}</td><td className="px-5 py-3 font-mono text-xs">{x.accountingVoucherNumber||x.accountingVoucherId||"—"}</td><td className="px-5 py-3 text-right"><button onClick={()=>setViewing(x)} className="rounded-md px-2.5 py-1.5 text-xs font-semibold text-[#465fff] hover:bg-[#f1f4ff]">View</button></td></tr>)}</tbody></table></div></section></div></main>{showForm&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 p-4"><form onSubmit={save} className="w-full max-w-xl overflow-hidden rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b px-5 py-4"><div><h2 className="font-bold">Post Expense</h2><p className="text-xs text-[#98a2b3]">Creates one balanced immutable voucher.</p></div><button type="button" onClick={()=>setShowForm(false)} className="text-xl text-[#667085]">×</button></div><div className="grid gap-4 p-5 sm:grid-cols-2"><label className="text-xs font-semibold">Expense number<input value={form.expenseNumber} onChange={e=>setForm({...form,expenseNumber:e.target.value})} className="mt-2 h-10 w-full rounded-lg border px-3 text-sm"/></label><label className="text-xs font-semibold">Date<input type="date" value={form.date} onChange={e=>setForm({...form,date:e.target.value})} className="mt-2 h-10 w-full rounded-lg border px-3 text-sm"/></label><label className="text-xs font-semibold">Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="mt-2 h-10 w-full rounded-lg border bg-white px-3 text-sm"><option>General</option>{categories.filter(c=>c.name&&c.name!=="General").map(c=><option key={c.id}>{c.name}</option>)}</select></label><label className="text-xs font-semibold">Amount<input required min="0.01" step="0.01" type="number" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} className="mt-2 h-10 w-full rounded-lg border px-3 text-sm"/></label><label className="text-xs font-semibold">Paid from<select value={form.paymentMethod} onChange={e=>setForm({...form,paymentMethod:e.target.value})} className="mt-2 h-10 w-full rounded-lg border bg-white px-3 text-sm"><option value="cash">Cash</option><option value="bank">Bank</option><option value="upi">UPI / Digital Wallet</option><option value="card">Card</option><option value="cheque">Cheque</option><option value="other">Other</option></select></label><label className="text-xs font-semibold">Bank account ID<input value={form.bankAccountId} onChange={e=>setForm({...form,bankAccountId:e.target.value})} disabled={form.paymentMethod==="cash"} className="mt-2 h-10 w-full rounded-lg border px-3 text-sm disabled:bg-[#f2f4f7]" placeholder="acct-bank"/></label><label className="text-xs font-semibold sm:col-span-2">Description<textarea rows={3} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="mt-2 w-full rounded-lg border px-3 py-2 text-sm"/></label></div><div className="flex justify-end gap-3 border-t px-5 py-4"><button type="button" onClick={()=>setShowForm(false)} className="rounded-lg border px-4 py-2 text-sm font-semibold">Cancel</button><button disabled={saving} className="erp-btn-primary rounded-lg px-5 py-2 text-sm font-semibold disabled:opacity-50">{saving?"Posting…":"Post Expense"}</button></div></form></div>}{viewing&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-black/30 p-4"><div className="w-full max-w-lg rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b px-5 py-4"><div><p className="text-xs font-semibold uppercase text-[#465fff]">Expense</p><h2 className="mt-1 font-bold">#{viewing.expenseNumber}</h2></div><button onClick={()=>setViewing(null)} className="text-xl text-[#667085]">×</button></div><div className="space-y-3 p-5 text-sm"><div className="flex justify-between"><span>Category</span><b>{viewing.categoryId}</b></div><div className="flex justify-between"><span>Date</span><b>{viewing.date}</b></div><div className="flex justify-between"><span>Paid from</span><b className="capitalize">{paymentNames[viewing.paymentMode||"cash"]||viewing.paymentMode}</b></div><div className="flex justify-between"><span>Amount</span><b>{money(Number(viewing.amountMinor||0))}</b></div><div className="rounded-lg bg-[#f8f9fc] p-4"><p className="text-xs text-[#667085]">Accounting voucher</p><p className="mt-1 font-mono text-xs">{viewing.accountingVoucherNumber||viewing.accountingVoucherId||"—"}</p></div><div><p className="text-xs text-[#667085]">Description</p><p className="mt-1">{viewing.description||"—"}</p></div></div><div className="flex justify-end border-t px-5 py-4"><button onClick={()=>setViewing(null)} className="rounded-lg bg-[#f2f4f7] px-4 py-2 text-sm font-semibold">Close</button></div></div></div>}</div></AuthGate>;
}
