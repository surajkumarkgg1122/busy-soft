"use client";

import { useEffect, useMemo, useState } from "react";
import { collection, getDocs } from "firebase/firestore";
import Sidebar from "./Components/Sidebar/page";
import TopNav from "./Components/TopNav/page";
import AuthGate from "./Components/Auth/AuthGate";
import { firestoreDb } from "../lib/firebase";
import { useBusiness } from "../context/BusinessContext";

const money=value=>`₹ ${Number(value||0).toLocaleString("en-IN",{maximumFractionDigits:2})}`;
const dateValue=value=>{if(!value)return null;if(typeof value==="string")return new Date(value);if(value?.toDate)return value.toDate();return value instanceof Date?value:null;};
const startFor=period=>{const d=new Date();d.setHours(0,0,0,0);if(period==="Today")return d;if(period==="This week"){const day=d.getDay()||7;d.setDate(d.getDate()-day+1);return d;}if(period==="This year"){d.setMonth(3,1);return d;}d.setDate(1);return d;};
function Metric({label,value,detail,tone="white"}){const styles={white:"border-[#e4e7ec] bg-white",dark:"border-[#1d2939] bg-[#101828] text-white",blue:"border-[#c8d4ff] bg-[#f1f4ff]",amber:"border-[#fedf89] bg-[#fffaeb]"};return <article className={`rounded-2xl border p-5 shadow-[0_1px_2px_rgba(16,24,40,0.04)] ${styles[tone]}`}><p className={`text-xs font-semibold uppercase tracking-[0.14em] ${tone==="dark"?"text-[#98a2b3]":"text-[#667085]"}`}>{label}</p><p className="mt-3 text-2xl font-semibold tracking-[-0.03em]">{value}</p><p className={`mt-2 text-xs ${tone==="dark"?"text-[#6ce9a6]":"text-[#667085]"}`}>{detail}</p></article>}
function BarChart({values}){const max=Math.max(...values.map(x=>x.value),1);return <div className="flex h-56 items-end gap-2">{values.map((x,i)=><div key={i} className="flex h-full flex-1 flex-col justify-end"><div title={`${x.label}: ${money(x.value)}`} className="w-full rounded-t-lg bg-[#465fff]" style={{height:`${Math.max(4,x.value/max*100)}%`}}/><span className="mt-2 truncate text-center text-[10px] text-[#98a2b3]">{x.label}</span></div>)}</div>}
export default function DashboardPage(){const {activeBusinessId,activeBusiness,loading:businessLoading,can}=useBusiness();const [sales,setSales]=useState([]),[customers,setCustomers]=useState([]),[expenses,setExpenses]=useState([]),[items,setItems]=useState([]),[period,setPeriod]=useState("This month"),[loading,setLoading]=useState(true),[error,setError]=useState("");const canView=can("reports","view")||can("sales","view");useEffect(()=>{async function load(){if(businessLoading)return;if(!firestoreDb||!activeBusinessId){setSales([]);setCustomers([]);setExpenses([]);setItems([]);setLoading(false);return;}if(!canView){setLoading(false);return;}setLoading(true);setError("");try{const businessRef=collection(firestoreDb,"businesses",activeBusinessId,"sales");const [s,c,e,i]=await Promise.all([getDocs(businessRef),getDocs(collection(firestoreDb,"businesses",activeBusinessId,"customers")),getDocs(collection(firestoreDb,"businesses",activeBusinessId,"expenses")),getDocs(collection(firestoreDb,"businesses",activeBusinessId,"items"))]);setSales(s.docs.map(d=>({id:d.id,...d.data()})));setCustomers(c.docs.map(d=>({id:d.id,...d.data()})));setExpenses(e.docs.map(d=>({id:d.id,...d.data()})));setItems(i.docs.map(d=>({id:d.id,...d.data()})));}catch(err){console.error(err);setError("Could not load dashboard data. Check your Firestore rules.");}finally{setLoading(false)}}load()},[activeBusinessId,businessLoading,canView]);const start=startFor(period);const filteredSales=useMemo(()=>sales.filter(s=>{const d=dateValue(s.invoiceDate||s.createdAt);return !d||d>=start}),[sales,start]);const filteredExpenses=useMemo(()=>expenses.filter(e=>{const d=dateValue(e.date||e.expenseDate||e.createdAt);return !d||d>=start}),[expenses,start]);const totalSales=filteredSales.reduce((a,s)=>a+Number(s.total||0),0),paid=filteredSales.reduce((a,s)=>a+Number(s.paid||0),0),outstanding=customers.reduce((a,c)=>a+Math.max(0,Number(c.balance||0)),0),expenseTotal=filteredExpenses.reduce((a,e)=>a+Number(e.amount||e.total||0),0),profit=totalSales-expenseTotal,pending=filteredSales.filter(s=>Number(s.balance||0)>0||["Pending","Unpaid","Partially Paid"].includes(s.status)).length,collectionRate=totalSales?Math.round(paid/totalSales*100):0;const chart=useMemo(()=>{const buckets=[];if(period==="Today"){for(let h=0;h<24;h++)buckets.push({label:`${h}:00`,value:0})}else{const days=period==="This week"?7:period==="This year"?12:30;for(let i=days-1;i>=0;i--)buckets.push({label:period==="This year"?new Date(new Date().getFullYear(),new Date().getMonth()-i,1).toLocaleString("en-IN",{month:"short"}):String(new Date(Date.now()-i*86400000).getDate()),value:0})}filteredSales.forEach(s=>{const d=dateValue(s.invoiceDate||s.createdAt);if(!d)return;let index;if(period==="Today")index=d.getHours();else if(period==="This year")index=buckets.length-1-(new Date().getMonth()-d.getMonth()+12)%12;else index=buckets.length-1-Math.floor((Date.now()-d.getTime())/86400000);if(index>=0&&index<buckets.length)buckets[index].value+=Number(s.total||0)});return buckets},[filteredSales,period]);const recent=[...filteredSales].sort((a,b)=>(dateValue(b.invoiceDate||b.createdAt)?.getTime()||0)-(dateValue(a.invoiceDate||a.createdAt)?.getTime()||0)).slice(0,6);const lowStock=items.filter(i=>Number(i.stock||0)<=Number(i.minStock||i.minimumStock||0)).slice(0,5);const businessName=activeBusiness?.business?.name||"Your business";return <AuthGate><div className="flex min-h-screen bg-[#f5f7fb]"><Sidebar/><main className="min-w-0 flex-1 px-5 pb-10 sm:px-8 lg:px-10"><TopNav/><div className="mx-auto max-w-[1440px]"><header className="flex flex-col justify-between gap-4 border-b border-[#e4e7ec] pb-6 pt-2 lg:flex-row lg:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#465fff]">Dashboard</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.04em] text-[#101828]">Business Command Center</h1><p className="mt-2 text-sm text-[#667085]">Live performance for <span className="font-semibold text-[#344054]">{businessName}</span>.</p></div><div className="flex flex-wrap gap-2"><select value={period} onChange={e=>setPeriod(e.target.value)} className="h-10 rounded-lg border border-[#d0d5dd] bg-white px-3 text-sm text-[#344054]"><option>Today</option><option>This week</option><option>This month</option><option>This year</option></select><a href="/sales" className="inline-flex h-10 items-center gap-2 rounded-lg bg-[#465fff] px-4 text-sm font-semibold text-white">+ Quick sale</a></div></header>{error&&<div className="mt-5 rounded-xl border border-[#fecdca] bg-[#fef3f2] px-4 py-3 text-sm text-[#b42318]">{error}</div>}<section className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Net sales" value={loading?"—":money(totalSales)} detail={`${filteredSales.length} invoices in period`} tone="dark"/><Metric label="Estimated profit" value={loading?"—":money(profit)} detail={`${totalSales?Math.round(profit/totalSales*100):0}% estimated margin`}/><Metric label="Collected" value={loading?"—":money(paid)} detail={`${collectionRate}% collection rate`} tone="blue"/><Metric label="Outstanding" value={loading?"—":money(outstanding)} detail={`${pending} invoices need follow-up`} tone="amber"/></section><section className="mt-5 grid gap-5 xl:grid-cols-[minmax(0,1.6fr)_minmax(300px,.8fr)]"><article className="rounded-2xl border border-[#e4e7ec] bg-white p-5 shadow-sm sm:p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#98a2b3]">Performance</p><h2 className="mt-1 text-lg font-semibold text-[#101828]">Sales trend</h2></div><span className="text-xs text-[#98a2b3]">{period}</span></div><div className="mt-7">{loading?<div className="flex h-56 items-center justify-center text-sm text-[#98a2b3]">Loading business data…</div>:<BarChart values={chart}/>}</div></article><article className="rounded-2xl border border-[#e4e7ec] bg-white p-5 shadow-sm sm:p-6"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#98a2b3]">Action center</p><h2 className="mt-1 text-lg font-semibold text-[#101828]">Needs attention</h2><div className="mt-4 divide-y divide-[#eaecf0]"><a href="/payments" className="block py-4"><p className="text-sm font-semibold text-[#344054]">Pending collections</p><p className="mt-1 text-xs text-[#98a2b3]">{pending} invoice{pending===1?"":"s"} with outstanding balance</p></a><a href="/items" className="block py-4"><p className="text-sm font-semibold text-[#344054]">Stock review</p><p className="mt-1 text-xs text-[#98a2b3]">{lowStock.length} item{lowStock.length===1?"":"s"} at/below minimum stock</p></a><a href="/customers" className="block py-4"><p className="text-sm font-semibold text-[#344054]">Receivables</p><p className="mt-1 text-xs text-[#98a2b3]">{money(outstanding)} total customer balance</p></a></div></article></section><section className="mt-5 grid gap-5 xl:grid-cols-[minmax(0,1.5fr)_minmax(320px,.7fr)]"><article className="overflow-hidden rounded-2xl border border-[#e4e7ec] bg-white shadow-sm"><div className="flex items-center justify-between border-b border-[#eaecf0] px-5 py-4"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#98a2b3]">Transactions</p><h2 className="mt-1 text-lg font-semibold text-[#101828]">Recent sales</h2></div><a href="/sales" className="text-xs font-semibold text-[#465fff]">View all →</a></div><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-sm"><thead className="bg-[#f8f9fc] text-left text-xs text-[#667085]"><tr><th className="px-5 py-3">Invoice</th><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Date</th><th className="px-5 py-3 text-right">Amount</th><th className="px-5 py-3">Status</th></tr></thead><tbody>{recent.length?recent.map(s=><tr key={s.id} className="border-t border-[#f2f4f7]"><td className="px-5 py-3 font-semibold text-[#344054]">#{s.invoiceNumber||s.id.slice(0,6)}</td><td className="px-5 py-3 text-[#667085]">{s.customerName||"Cash Sale"}</td><td className="px-5 py-3 text-[#667085]">{dateValue(s.invoiceDate||s.createdAt)?.toLocaleDateString("en-IN")||"—"}</td><td className="px-5 py-3 text-right font-semibold text-[#344054]">{money(s.total)}</td><td className="px-5 py-3"><span className={`rounded-full px-2 py-1 text-[11px] font-semibold ${Number(s.balance||0)>0?"bg-[#fffaeb] text-[#b54708]":"bg-[#ecfdf3] text-[#067647]"}`}>{s.status||"Paid"}</span></td></tr>):<tr><td colSpan="5" className="px-5 py-10 text-center text-sm text-[#98a2b3]">No sales found for this period.</td></tr>}</tbody></table></div></article><article className="rounded-2xl border border-[#e4e7ec] bg-white p-5 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#98a2b3]">Inventory</p><h2 className="mt-1 text-lg font-semibold text-[#101828]">Stock watch</h2><div className="mt-4 space-y-3">{lowStock.length?lowStock.map(i=><div key={i.id} className="flex items-center gap-3 rounded-xl bg-[#f8f9fc] p-3"><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold text-[#344054]">{i.name||"Unnamed item"}</p><p className="text-xs text-[#98a2b3]">Minimum {Number(i.minStock||i.minimumStock||0)}</p></div><span className="rounded-lg bg-[#fef3f2] px-2 py-1 text-xs font-bold text-[#b42318]">{Number(i.stock||0)} left</span></div>):<div className="rounded-xl bg-[#ecfdf3] p-4 text-sm text-[#067647]">All tracked items are above their minimum stock level.</div>}</div><a href="/items" className="mt-4 inline-block text-xs font-semibold text-[#465fff]">Open inventory →</a></article></section></div></main></div></AuthGate>}
