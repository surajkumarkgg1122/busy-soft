"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
export default function JoinExistingCompanyPage(){const router=useRouter();const[invitationId,setInvitationId]=useState("");const[error,setError]=useState("");function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const id=invitationId.trim();if(!id){setError("Enter the invitation ID from your company administrator.");return;}setError("");router.push(`/join/${encodeURIComponent(id)}`);}return <main className="min-h-screen bg-[#f5f7fb] px-5 py-10"><div className="mx-auto flex min-h-[calc(100vh-5rem)] max-w-lg items-center justify-center"><section className="w-full rounded-3xl border border-[#e4e7ec] bg-white p-8 shadow-[0_20px_60px_rgba(16,24,40,0.08)] sm:p-10"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-[#eef2ff] text-xl font-black text-[#465fff]">B</div><p className="mt-5 text-center text-xs font-semibold uppercase tracking-[0.16em] text-[#465fff]">Join an existing company</p><h1 className="mt-2 text-center text-2xl font-semibold tracking-[-0.03em] text-[#101828]">Enter your invitation ID</h1><p className="mt-3 text-center text-sm leading-6 text-[#667085]">Your company administrator can provide the invitation ID. We’ll verify the invitation, email address, role and expiry before you join.</p><form onSubmit={submit} className="mt-7"><label htmlFor="invitationId" className="text-sm font-semibold text-[#344054]">Invitation ID</label><input id="invitationId" autoFocus value={invitationId} onChange={e=>setInvitationId(e.target.value)} placeholder="e.g. inv_8f3..." className="mt-2 h-12 w-full rounded-xl border border-[#d0d5dd] px-4 text-sm outline-none focus:border-[#465fff] focus:ring-4 focus:ring-[#465fff]/10"/>{error&&<p role="alert" className="mt-3 rounded-xl border border-[#fecdca] bg-[#fef3f2] px-4 py-3 text-sm text-[#b42318]">{error}</p>}<button type="submit" className="mt-5 h-12 w-full rounded-xl bg-[#465fff] px-5 text-sm font-semibold text-white">Continue</button></form><div className="mt-6 flex items-center justify-between text-sm"><Link href="/onboarding/business" className="font-semibold text-[#465fff]">Create a new business</Link><Link href="/" className="font-semibold text-[#667085]">Back</Link></div></section></div></main>}
