"use client";

import { useEffect, useMemo, useState } from "react";
import { addDoc, collection, deleteDoc, doc, getDocs, serverTimestamp, updateDoc } from "firebase/firestore";
import Sidebar from "../Components/Sidebar/page";
import TopNav from "../Components/TopNav/page";
import AuthGate from "../Components/Auth/AuthGate";
import { firestoreDb } from "../../lib/firebase";

type Item = { id: string; name?: string; code?: string; unit?: string; salePrice?: number; purchasePrice?: number; stock?: number; minStock?: number; location?: string; itemType?: string };
type ItemForm = { name: string; code: string; unit: string; salePrice: string; purchasePrice: string; stock: string; minStock: string; location: string; itemType: string };
const blank: ItemForm = { name: "", code: "", unit: "Piece", salePrice: "", purchasePrice: "", stock: "", minStock: "", location: "", itemType: "Product" };
const icon = { search: <><circle cx="11" cy="11" r="6.5" /><path d="m16 16 4.5 4.5" /></>, plus: <><path d="M12 5v14M5 12h14" /></>, box: <><path d="m4 8 8-4 8 4-8 4-8-4Z" /><path d="M4 8v8l8 4 8-4V8M12 12v8" /></>, close: <path d="m6 6 12 12M18 6 6 18" /> };
function Icon({ name }: { name: keyof typeof icon }) { return <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.9" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">{icon[name]}</svg>; }
const money = (value: number) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(value);

export default function ItemsPage() {
  const [items, setItems] = useState<Item[]>([]);
  const [selected, setSelected] = useState<Item | null>(null);
  const [query, setQuery] = useState("");
  const [filter, setFilter] = useState("All items");
  const [form, setForm] = useState<ItemForm>(blank);
  const [step, setStep] = useState(1);
  const [showForm, setShowForm] = useState(false);
  const [editing, setEditing] = useState<Item | null>(null);
  const [adjusting, setAdjusting] = useState(false);
  const [adjustmentAmount, setAdjustmentAmount] = useState("");
  const [adjustmentMode, setAdjustmentMode] = useState<"add" | "remove">("add");
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState("");

  useEffect(() => { async function loadItems() {
    if (!firestoreDb) { setError("Firebase is not configured. Check your .env.local file."); setLoading(false); return; }
    try { const snapshot = await getDocs(collection(firestoreDb, "items")); setItems(snapshot.docs.map((record) => ({ id: record.id, ...record.data() })) as Item[]); }
    catch (reason) { console.error(reason); setError("Could not load inventory. Enable Firestore and check its rules."); }
    finally { setLoading(false); }
  } loadItems(); }, []);

  const filtered = useMemo(() => items.filter((item) => {
    const lowStock = Number(item.stock || 0) <= Number(item.minStock || 0);
    return `${item.name} ${item.code}`.toLowerCase().includes(query.toLowerCase()) && (filter === "All items" || filter === "Low stock" && lowStock || filter === "Out of stock" && !Number(item.stock || 0));
  }), [filter, items, query]);
  const totals = useMemo(() => ({ total: items.length, low: items.filter((item) => Number(item.stock || 0) <= Number(item.minStock || 0)).length, empty: items.filter((item) => !Number(item.stock || 0)).length, value: items.reduce((sum, item) => sum + Number(item.stock || 0) * Number(item.purchasePrice || 0), 0) }), [items]);
  const update = (field: keyof ItemForm) => (event: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => setForm((current) => ({ ...current, [field]: event.target.value }));
  const openCreate = () => { setEditing(null); setForm(blank); setStep(1); setShowForm(true); setError(""); };
  const openEdit = () => {
    if (!selected) return;
    setEditing(selected);
    setForm({ name: selected.name || "", code: selected.code || "", unit: selected.unit || "Piece", salePrice: String(selected.salePrice || ""), purchasePrice: String(selected.purchasePrice || ""), stock: String(selected.stock || 0), minStock: String(selected.minStock || 0), location: selected.location || "", itemType: selected.itemType || "Product" });
    setStep(1); setShowForm(true);
  };
  async function remove() {
    if (!firestoreDb || !selected || !window.confirm(`Delete ${selected.name || "this item"}? This cannot be undone.`)) return;
    try { await deleteDoc(doc(firestoreDb, "items", selected.id)); setItems((current) => current.filter((item) => item.id !== selected.id)); setSelected(null); }
    catch (reason) { console.error(reason); setError("Could not delete item."); }
  }
  async function saveAdjustment(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault();
    if (!firestoreDb || !selected) return;
    const amount = Number(adjustmentAmount);
    if (!amount || amount < 0) return;
    const stock = Math.max(0, Number(selected.stock || 0) + (adjustmentMode === "add" ? amount : -amount));
    try {
      await updateDoc(doc(firestoreDb, "items", selected.id), { stock, updatedAt: serverTimestamp() });
      const item = { ...selected, stock };
      setItems((current) => current.map((currentItem) => currentItem.id === item.id ? item : currentItem));
      setSelected(item); setAdjustmentAmount(""); setAdjusting(false);
    } catch (reason) { console.error(reason); setError("Could not adjust item quantity."); }
  }

  async function save(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); if (!firestoreDb) return; setSaving(true);
    try {
      const record = { ...form, stock: Number(form.stock) || 0, minStock: Number(form.minStock) || 0, salePrice: Number(form.salePrice) || 0, purchasePrice: Number(form.purchasePrice) || 0, createdAt: serverTimestamp(), updatedAt: serverTimestamp() };
      if (editing) {
        await updateDoc(doc(firestoreDb, "items", editing.id), record);
        const item = { ...editing, ...record }; setItems((current) => current.map((currentItem) => currentItem.id === editing.id ? item : currentItem)); setSelected(item);
      } else {
        const saved = await addDoc(collection(firestoreDb, "items"), record);
        const item = { ...record, id: saved.id }; setItems((current) => [item, ...current]); setSelected(item);
      }
      setShowForm(false); setEditing(null);
    } catch (reason) { console.error(reason); setError("Could not save item. Check Firestore rules and try again."); }
    finally { setSaving(false); }
  }

  const status = (item: Item) => !Number(item.stock || 0) ? ["Out of stock", "bg-[#fff1ed] text-[#d45c3c]"] : Number(item.stock || 0) <= Number(item.minStock || 0) ? ["Low stock", "bg-[#fff7e8] text-[#b7791f]"] : ["In stock", "bg-[#e8f8f1] text-[#168361]"];

  return <AuthGate><div className="flex min-h-screen bg-[#f8f7f4]"><Sidebar /><main className="min-w-0 flex-1 px-4 pb-10 pt-0 sm:px-6 lg:px-8"><TopNav /><div className="mx-auto max-w-[1450px]">
    <section className="mb-8 flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><p className="text-sm font-semibold text-[#4f46e5]">Inventory control</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-[#182230]">Items</h1><p className="mt-2 max-w-xl text-sm text-[#667085]">Keep your stock, pricing, and reorder levels in one clear workspace.</p></div><div className="flex flex-wrap gap-3"><button type="button" className="h-10 rounded-xl border border-[#d0d5dd] bg-white px-4 text-sm font-semibold text-[#344054] hover:bg-[#f9fafb]">Export inventory</button><button type="button" onClick={openCreate} className="inline-flex h-10 items-center gap-2 rounded-xl bg-[#4f46e5] px-4 text-sm font-semibold text-white shadow-[0_8px_18px_rgba(79,70,229,.22)] hover:bg-[#4338ca]"><span className="h-4 w-4"><Icon name="plus" /></span>New item</button></div></section>
    {error && <div role="alert" className="mb-5 rounded-xl border border-[#fecdca] bg-[#fef3f2] px-4 py-3 text-sm text-[#b42318]">{error}</div>}
    <section className="mb-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[[String(totals.total), "Total items", "Across your catalogue", "text-[#4f46e5]"], [String(totals.low), "Low stock", "Needs a reorder soon", "text-[#b7791f]"], [String(totals.empty), "Out of stock", "Unavailable to sell", "text-[#d45c3c]"], [money(totals.value), "Inventory value", "At purchase cost", "text-[#168361]"]].map(([value, label, note, tone]) => <article key={label} className="rounded-2xl border border-[#e7e5e4] bg-white p-5 shadow-[0_1px_2px_rgba(16,24,40,.04)]"><p className="text-sm font-medium text-[#667085]">{label}</p><p className={`mt-3 text-2xl font-bold tracking-tight ${tone}`}>{value}</p><p className="mt-1 text-xs text-[#98a2b3]">{note}</p></article>)}</section>
    <section className="overflow-hidden rounded-2xl border border-[#e7e5e4] bg-white shadow-[0_3px_10px_rgba(16,24,40,.04)]"><div className="flex flex-col justify-between gap-4 border-b border-[#eaecf0] p-5 lg:flex-row lg:items-center"><div><h2 className="font-bold text-[#182230]">Catalogue</h2><p className="mt-1 text-sm text-[#667085]">{filtered.length} item{filtered.length === 1 ? "" : "s"} shown</p></div><div className="flex flex-col gap-3 sm:flex-row"><label className="relative"><span className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[#98a2b3]"><Icon name="search" /></span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search items or SKU" className="h-10 w-full rounded-xl border border-[#d0d5dd] pl-9 pr-3 text-sm outline-none focus:border-[#4f46e5] sm:w-60" /></label><select value={filter} onChange={(event) => setFilter(event.target.value)} className="h-10 rounded-xl border border-[#d0d5dd] bg-white px-3 text-sm text-[#344054] outline-none"><option>All items</option><option>Low stock</option><option>Out of stock</option></select></div></div>
      {loading ? <p className="p-16 text-center text-sm text-[#667085]">Loading inventory…</p> : filtered.length === 0 ? <div className="flex min-h-80 flex-col items-center justify-center p-10 text-center"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-[#eeedff] text-[#4f46e5]"><span className="h-7 w-7"><Icon name="box" /></span></div><h3 className="mt-4 font-bold text-[#182230]">Your inventory is ready for its first item</h3><p className="mt-2 max-w-sm text-sm text-[#667085]">Create an item to track its price, location, and stock level.</p><button type="button" onClick={openCreate} className="mt-5 text-sm font-bold text-[#4f46e5] hover:underline">Create item</button></div> : <div className="overflow-x-auto"><table className="min-w-[860px] text-left"><thead className="bg-[#fbfaf9] text-xs font-semibold uppercase tracking-wide text-[#667085]"><tr>{["Item", "Unit", "Stock", "Cost", "Sale price", "Status", ""].map((heading) => <th key={heading} className="px-5 py-3">{heading}</th>)}</tr></thead><tbody className="divide-y divide-[#eaecf0]">{filtered.map((item) => { const [label, tone] = status(item); const percentage = Math.min(100, Number(item.stock || 0) / Math.max(1, Number(item.minStock || 0) * 2) * 100); return <tr key={item.id} className="cursor-pointer transition hover:bg-[#fafafa]" onClick={() => setSelected(item)}><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#eeedff] text-[#4f46e5]"><span className="h-5 w-5"><Icon name="box" /></span></span><span><span className="block font-semibold text-[#182230]">{item.name || "Unnamed item"}</span><span className="mt-0.5 block text-xs text-[#98a2b3]">SKU: {item.code || "—"}</span></span></div></td><td className="px-5 py-4 text-sm text-[#667085]">{item.unit || "Piece"}</td><td className="px-5 py-4"><div className="w-24"><div className="flex justify-between text-xs font-semibold text-[#475467]"><span>{Number(item.stock || 0)}</span><span>min {Number(item.minStock || 0)}</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-[#eef0f3]"><span className={`block h-full rounded-full ${label === "In stock" ? "bg-[#2bb584]" : label === "Low stock" ? "bg-[#e8a333]" : "bg-[#e36d54]"}`} style={{ width: `${percentage}%` }} /></div></div></td><td className="px-5 py-4 text-sm text-[#475467]">{money(Number(item.purchasePrice || 0))}</td><td className="px-5 py-4 text-sm font-semibold text-[#182230]">{money(Number(item.salePrice || 0))}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${tone}`}>{label}</span></td><td className="px-5 py-4 text-right text-[#98a2b3]">›</td></tr>; })}</tbody></table></div>}</section>
  </div></main></div>
  {selected&&<aside className="fixed inset-y-0 right-0 z-40 flex w-full max-w-md flex-col border-l border-[#e4e7ec] bg-white shadow-2xl"><header className="flex justify-between border-b border-[#eaecf0] p-6"><div><p className="text-sm font-semibold text-[#4f46e5]">Item details</p><h2 className="mt-1 text-xl font-bold text-[#182230]">{selected.name}</h2><p className="mt-1 text-sm text-[#667085]">SKU: {selected.code||"Not assigned"}</p></div><button onClick={()=>setSelected(null)} className="text-2xl text-[#667085]">×</button></header><div className="flex-1 p-6"><div className="grid grid-cols-2 gap-3"><div className="rounded-xl bg-[#f6f5ff] p-4"><p className="text-xs text-[#667085]">AVAILABLE</p><p className="mt-2 text-2xl font-bold text-[#4f46e5]">{selected.stock||0} {selected.unit}</p></div><div className="rounded-xl bg-[#ecfaf4] p-4"><p className="text-xs text-[#667085]">STOCK VALUE</p><p className="mt-2 text-xl font-bold text-[#168361]">{money(Number(selected.stock||0)*Number(selected.purchasePrice||0))}</p></div></div><dl className="mt-6 divide-y rounded-xl border border-[#eaecf0] text-sm"><div className="flex justify-between p-3"><dt>Sell price</dt><dd>{money(Number(selected.salePrice||0))}</dd></div><div className="flex justify-between p-3"><dt>Purchase cost</dt><dd>{money(Number(selected.purchasePrice||0))}</dd></div></dl></div><footer className="space-y-3 border-t border-[#eaecf0] p-5"><button onClick={()=>setAdjusting(true)} className="w-full rounded-xl bg-[#4f46e5] py-3 text-sm font-semibold text-white">Adjust quantity</button><div className="grid grid-cols-2 gap-3"><button onClick={openEdit} className="rounded-xl border border-[#d0d5dd] py-2.5 text-sm font-semibold text-[#344054]">Modify item</button><button onClick={remove} className="rounded-xl border border-[#f7c8bc] py-2.5 text-sm font-semibold text-[#c13c25]">Delete item</button></div></footer></aside>}
  {showForm && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#182230]/45 p-4"><form onSubmit={save} className="flex max-h-[90vh] w-full max-w-4xl flex-col overflow-hidden rounded-2xl bg-white shadow-2xl"><header className="flex items-center justify-between border-b border-[#eaecf0] px-7 py-5"><div><p className="text-sm font-semibold text-[#4f46e5]">New catalogue item</p><h2 className="mt-1 text-xl font-bold text-[#182230]">Create an item</h2></div><button type="button" onClick={() => setShowForm(false)} aria-label="Close item form" className="rounded-lg p-2 text-[#667085] hover:bg-[#f4f3f1]"><span className="block h-5 w-5"><Icon name="close" /></span></button></header><div className="grid min-h-0 flex-1 md:grid-cols-[190px_1fr]"><aside className="border-b border-[#eaecf0] bg-[#fbfaf9] p-5 md:border-b-0 md:border-r">{[["1", "Basic information"], ["2", "Pricing & tax"], ["3", "Stock settings"], ["4", "Review"]].map(([number, label]) => <button type="button" key={number} onClick={() => setStep(Number(number))} className={`mb-2 flex w-full items-center gap-3 rounded-xl p-3 text-left text-sm font-semibold ${step === Number(number) ? "bg-[#eeedff] text-[#4f46e5]" : "text-[#667085] hover:bg-white"}`}><span className={`flex h-6 w-6 items-center justify-center rounded-full text-xs ${step === Number(number) ? "bg-[#4f46e5] text-white" : "bg-[#eaecf0]"}`}>{number}</span>{label}</button>)}</aside><div className="overflow-y-auto p-7">{step === 1 && <div className="grid gap-5 sm:grid-cols-2"><label className="sm:col-span-2 text-sm font-semibold text-[#344054]">Item name<input required value={form.name} onChange={update("name")} placeholder="e.g. Mineral water jar" className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] px-3 font-normal outline-none focus:border-[#4f46e5]" /></label><label className="text-sm font-semibold text-[#344054]">SKU / item code<input value={form.code} onChange={update("code")} placeholder="Optional code" className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] px-3 font-normal outline-none focus:border-[#4f46e5]" /></label><label className="text-sm font-semibold text-[#344054]">Unit<select value={form.unit} onChange={update("unit")} className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] bg-white px-3 font-normal outline-none focus:border-[#4f46e5]"><option>Piece</option><option>Box</option><option>Kg</option><option>Litre</option></select></label><label className="text-sm font-semibold text-[#344054]">Item type<select value={form.itemType} onChange={update("itemType")} className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] bg-white px-3 font-normal outline-none focus:border-[#4f46e5]"><option>Product</option><option>Service</option></select></label><label className="text-sm font-semibold text-[#344054]">Storage location<input value={form.location} onChange={update("location")} placeholder="e.g. Main warehouse" className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] px-3 font-normal outline-none focus:border-[#4f46e5]" /></label></div>}{step === 2 && <div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-[#344054]">Selling price<input type="number" min="0" value={form.salePrice} onChange={update("salePrice")} placeholder="0.00" className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] px-3 font-normal outline-none focus:border-[#4f46e5]" /></label><label className="text-sm font-semibold text-[#344054]">Purchase cost<input type="number" min="0" value={form.purchasePrice} onChange={update("purchasePrice")} placeholder="0.00" className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] px-3 font-normal outline-none focus:border-[#4f46e5]" /></label></div>}{step === 3 && <div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-[#344054]">Opening stock<input type="number" min="0" value={form.stock} onChange={update("stock")} placeholder="0" className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] px-3 font-normal outline-none focus:border-[#4f46e5]" /></label><label className="text-sm font-semibold text-[#344054]">Reorder level<input type="number" min="0" value={form.minStock} onChange={update("minStock")} placeholder="0" className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] px-3 font-normal outline-none focus:border-[#4f46e5]" /></label></div>}{step === 4 && <div className="rounded-2xl bg-[#f6f5ff] p-6"><p className="text-sm font-semibold text-[#4f46e5]">Ready to save</p><h3 className="mt-2 text-xl font-bold text-[#182230]">{form.name || "Untitled item"}</h3><p className="mt-2 text-sm text-[#667085]">{form.stock || 0} {form.unit} · Sale price {money(Number(form.salePrice || 0))}</p></div>}</div></div><footer className="flex items-center justify-between border-t border-[#eaecf0] px-7 py-5"><button type="button" onClick={() => setStep((current) => Math.max(1, current - 1))} className="text-sm font-semibold text-[#667085] disabled:opacity-50" disabled={step === 1}>Back</button>{step < 4 ? <button type="button" onClick={() => setStep((current) => current + 1)} className="rounded-xl bg-[#4f46e5] px-5 py-2.5 text-sm font-semibold text-white">Continue</button> : <button disabled={saving} type="submit" className="rounded-xl bg-[#4f46e5] px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{saving ? "Saving…" : "Save item"}</button>}</footer></form></div>}
  {adjusting && selected && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-[#182230]/45 p-4"><form onSubmit={saveAdjustment} className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-sm font-semibold text-[#4f46e5]">Stock control</p><h2 className="mt-1 text-xl font-bold text-[#182230]">Adjust quantity</h2><p className="mt-2 text-sm text-[#667085]">Current: {Number(selected.stock || 0)} {selected.unit || "units"}</p></div><button type="button" onClick={() => setAdjusting(false)} aria-label="Close adjustment" className="text-2xl text-[#667085]">×</button></div><div className="mt-6 flex rounded-xl bg-[#f4f3f1] p-1"><button type="button" onClick={() => setAdjustmentMode("add")} className={`flex-1 rounded-lg py-2 text-sm font-semibold ${adjustmentMode === "add" ? "bg-white text-[#4f46e5] shadow-sm" : "text-[#667085]"}`}>Add stock</button><button type="button" onClick={() => setAdjustmentMode("remove")} className={`flex-1 rounded-lg py-2 text-sm font-semibold ${adjustmentMode === "remove" ? "bg-white text-[#c13c25] shadow-sm" : "text-[#667085]"}`}>Remove stock</button></div><label className="mt-5 block text-sm font-semibold text-[#344054]">Quantity<input required min="1" type="number" value={adjustmentAmount} onChange={(event) => setAdjustmentAmount(event.target.value)} className="mt-2 h-11 w-full rounded-xl border border-[#d0d5dd] px-3 font-normal outline-none focus:border-[#4f46e5]" /></label><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setAdjusting(false)} className="px-4 text-sm font-semibold text-[#667085]">Cancel</button><button type="submit" className="rounded-xl bg-[#4f46e5] px-5 py-2.5 text-sm font-semibold text-white">Save adjustment</button></div></form></div>}
  </AuthGate>;
}
